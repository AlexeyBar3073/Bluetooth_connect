#pragma once

// -----------------------------------------------------------------------------
// app_config.h
// Общие константы проекта: пины, диапазоны моделей и параметры поведения.
// Идея: этот файл почти не меняется при замене симулятора на "реальное железо".
// -----------------------------------------------------------------------------

// Аппаратный ввод (симулятор): потенциометр "газа"
#define POTENTIOMETER_PIN 33

// Модель скорости (для телеметрии): максимум 220 км/ч
#define SPEED_MAX 220.0f

// Максимальное значение АЦП для конфигурации analogReadResolution(12)
#define ADC_MAX 4095.0f

// RPM на холостом ходе (минимальный базовый уровень оборотов)
#define RPM_IDLE 750.0f

// Максимальные обороты (верхний предел для кривой RPM->напряжение)
#define RPM_MAX 6500.0f

// "Напряжение холостого хода" (нижняя точка маппинга RPM->voltage)
#define VOLTAGE_IDLE 11.0f

// Длина импульса форсунки в миллисекундах (в модели топлива)
#define INJECTOR_PULSE_MS 7.0f

// Размер стека FreeRTOS-задач
#define TASK_STACK_SIZE 8192

// Защита от "дребезга" переподключений Bluetooth (в миллисекундах)
#define CONNECTION_DEBOUNCE_MS 500

// -----------------------------------------------------------------------------
// Параметры протокола телеметрии / отправки по Bluetooth
// -----------------------------------------------------------------------------

// Минимальный интервал между "обычными" отправками телеметрии
static const unsigned long MIN_SEND_INTERVAL_MS = 50;

// Порог изменения скорости (когда нужно форсировать отправку)
static const float SPEED_CHANGE_THRESHOLD = 0.1f;

// Порог изменения RPM (когда нужно форсировать отправку)
static const float RPM_CHANGE_THRESHOLD = 5.0f;

// Порог изменения уровня топлива (л), когда нужно форсировать отправку
static const float FUEL_CHANGE_THRESHOLD = 0.05f;
