#pragma once

// -----------------------------------------------------------------------------
// runtime.h
// Вспомогательные функции "в рантайме": старт/стоп вычислений и обновление
// производных величин (например, уровня топлива).
// -----------------------------------------------------------------------------

// stopAllCalculations: останавливает стриминг, сбрасывает параметры и фильтры.
void stopAllCalculations();

// startAllCalculations: включает стриминг и обновляет внутренние таймеры.
void startAllCalculations();

// updateFuelLevel: пересчитывает текущий уровень топлива на основе fuel_used_total.
void updateFuelLevel();

// getConnectionState: возвращает флаг активного BT-клиента.
bool getConnectionState();
