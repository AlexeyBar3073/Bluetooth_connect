#pragma once

// -----------------------------------------------------------------------------
// vehicle_model.h
// "Домен" модели автомобиля: расчёт виртуальных параметров (RPM, voltage, gear)
// на основе скорости.
// В будущем: эта модель заменится/будет обогащена реальными данными с ЭБУ.
// -----------------------------------------------------------------------------

// calculateRPM: вычисляет RPM по скорости (км/ч) по кусочно-линейной кривой.
float calculateRPM(float speed);

// calculateVoltage: вычисляет напряжение по RPM (через map из Arduino).
float calculateVoltage(float rpm);

// calculateGear: рассчитывает передачу (1..4) по скорости.
int calculateGear(float speed);
